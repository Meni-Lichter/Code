// This file is the entry point of the application. It initializes the application and sets up any necessary configurations.

import { initializeApp } from './services'; // Importing the service to initialize the app

const app = initializeApp(); // Initialize the application

app.start(); // Start the application